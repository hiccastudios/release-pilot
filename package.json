{
  "name": "hicca-release-pilot",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "helper": "node helper/server.mjs",
    "check": "node --check main.js && node --check helper/server.mjs && node --check helper/amuse-automation.mjs"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
